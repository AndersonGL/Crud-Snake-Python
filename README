# 🐍 CRUD Snake

Um projeto **Django** simples que exemplifica a criação de um CRUD (Create • Read • Update • Delete) para cadastro de **Carros**, com views básicas em função, templates Bootstrap e banco de dados padrão SQLite.

---

## ✨ Visão Geral

> Demonstra os passos essenciais para iniciar um projeto Django, criar um app, modelar dados e construir páginas de listagem e formulário usando Bootstrap. Ótimo ponto de partida para quem deseja evoluir para uma aplicação completa ou estudar a stack **Python + Django**.

---

## 🚀 Tecnologias & Ferramentas

| Camada          | Tech / Tool                                                                     |
|-----------------|---------------------------------------------------------------------------------|
| **Linguagem**   | Python 3.12+                                                                    |
| **Framework**   | Django 5.x                                                                      |
| **Front-end**   | HTML5 • CSS3 (Bootstrap v5)                                                     |
| **Ger. Ambiente** | `venv`                                                                        |
| **Banco Padrão**| SQLite (por padrão do Django)                                                   |
| **IDE**         | VS Code (recomendado)                                                           |

---

## ⚙️ Pré-requisitos

- Python instalado e atualizado  
- Pip atualizado (`python -m pip install --upgrade pip`)  
- Acesso a terminal/PowerShell/Bash  
- Git (opcional, mas recomendado)  

```bash
# Checar versão do Python
python --version

